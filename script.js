const header=document.querySelector('.site-header');const menu=document.querySelector('.menu');if(menu&&header){menu.addEventListener('click',()=>header.classList.toggle('menu-open'))}
const filters=document.querySelectorAll('.filter');const items=document.querySelectorAll('.gallery-item');filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;items.forEach(item=>{item.style.display=f==='all'||item.dataset.cat===f?'block':'none'})}));
const lightbox=document.querySelector('#lightbox');if(lightbox){const title=document.querySelector('#lb-title'),desc=document.querySelector('#lb-desc'),art=document.querySelector('#lightbox-art');document.querySelectorAll('.gallery-item').forEach(item=>item.addEventListener('click',()=>{art.innerHTML=item.querySelector('.mock-img').outerHTML;title.textContent=item.dataset.title;desc.textContent=item.dataset.desc;lightbox.classList.add('show');lightbox.setAttribute('aria-hidden','false')}));document.querySelector('.close').addEventListener('click',()=>lightbox.classList.remove('show'));lightbox.addEventListener('click',e=>{if(e.target===lightbox)lightbox.classList.remove('show')})}
const form=document.querySelector('#projectForm');if(form){const params=new URLSearchParams(location.search);const serviceParam=params.get('service');const serviceSelect=form.querySelector('[name=service]');if(serviceParam&&serviceSelect){const map={tattoo:'Tattoo / Custom Tattoo',painting:'Canvas Painting',pencil:'Pencil Artwork'};serviceSelect.value=map[serviceParam]||''}form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=data.get('name');const phone=data.get('phone');const service=data.get('service');const brief=data.get('brief');const ref=data.get('reference');const msg=`Hi Seidu, I'd like to discuss a project.%0A%0AName: ${encodeURIComponent(name)}%0AWhatsApp: ${encodeURIComponent(phone)}%0AService: ${encodeURIComponent(service)}%0A%0ABrief:%0A${encodeURIComponent(brief)}${ref?`%0A%0AReference: ${encodeURIComponent(ref)}`:''}`;document.querySelector('#formMessage').textContent='Opening WhatsApp with your project brief…';window.open(`https://wa.me/233257231812?text=${msg}`,'_blank')})}