const header=document.querySelector('.site-header');const menu=document.querySelector('.menu');if(menu&&header){menu.addEventListener('click',()=>{const open=header.classList.toggle('menu-open');menu.setAttribute('aria-expanded',String(open));});}const progress=document.createElement('div');progress.className='reading-progress';document.body.prepend(progress);const update=()=>{const total=document.documentElement.scrollHeight-window.innerHeight;progress.style.setProperty('--progress',`${total>0?window.scrollY/total*100:0}%`);header?.classList.toggle('is-scrolled',window.scrollY>24)};addEventListener('scroll',update,{passive:true});update();const targets=document.querySelectorAll('main>section,.work-card,.gallery-item,.service-row,.steps>div,.about-copy>*,.contact-info>div');targets.forEach((el,i)=>{el.classList.add('reveal');el.style.setProperty('--reveal-delay',`${Math.min(i%5,4)*55}ms`)});if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const observer=new IntersectionObserver((entries,obs)=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');obs.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -6% 0px'});targets.forEach(el=>observer.observe(el))}else targets.forEach(el=>el.classList.add('is-visible'));const filters=document.querySelectorAll('.filter');const items=document.querySelectorAll('.gallery-item');filters.forEach(button=>button.addEventListener('click',()=>{filters.forEach(item=>item.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;items.forEach(item=>{const visible=filter==='all'||item.dataset.cat===filter;item.hidden=!visible;if(visible)requestAnimationFrame(()=>item.classList.add('is-visible'))})}));const lightbox=document.querySelector('#lightbox');if(lightbox){const title=document.querySelector('#lb-title'),desc=document.querySelector('#lb-desc'),art=document.querySelector('#lightbox-art'),close=lightbox.querySelector('.close');const closeBox=()=>{lightbox.classList.remove('show');lightbox.setAttribute('aria-hidden','true')};document.querySelectorAll('.gallery-item').forEach(item=>item.addEventListener('click',()=>{art.innerHTML=item.querySelector('.mock-img').outerHTML;title.textContent=item.dataset.title;desc.textContent=item.dataset.desc;lightbox.classList.add('show');lightbox.setAttribute('aria-hidden','false');close?.focus()}));close?.addEventListener('click',closeBox);lightbox.addEventListener('click',e=>{if(e.target===lightbox)closeBox()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeBox()})}const form=document.querySelector('#projectForm');if(form){const params=new URLSearchParams(location.search),select=form.querySelector('[name="service"]'),map={tattoo:'Tattoo / Custom Tattoo',painting:'Canvas Painting',pencil:'Pencil Artwork'};if(params.get('service')&&select)select.value=map[params.get('service')]||'';form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form),value=k=>String(data.get(k)||'');const message=["Hi Seidu, I'd like to discuss a project.",'',`Name: ${value('name')}`,`WhatsApp: ${value('phone')}`,`Service: ${value('service')}`,'',`Brief:\n${value('brief')}`,value('reference')?`\nReference: ${value('reference')}`:''].join('\n');const status=document.querySelector('#formMessage');if(status)status.textContent='Opening WhatsApp with your project brief…';window.open(`https://wa.me/233257231812?text=${encodeURIComponent(message)}`,'_blank','noopener')})}
