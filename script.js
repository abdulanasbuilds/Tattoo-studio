const header=document.querySelector('.site-header');const menu=document.querySelector('.menu');if(menu&&header){menu.addEventListener('click',()=>{const open=header.classList.toggle('menu-open');menu.setAttribute('aria-expanded',String(open));});}const progress=document.createElement('div');progress.className='reading-progress';document.body.prepend(progress);const update=()=>{const total=document.documentElement.scrollHeight-innerHeight;progress.style.setProperty('--progress',`${total>0?scrollY/total*100:0}%`)};addEventListener('scroll',update,{passive:true});update();const els=document.querySelectorAll('main>section,.project-card,.service-item,.process-item,.stat,.team-card,.award-card,.faq-list details');els.forEach((el,i)=>{el.classList.add('reveal');el.style.transitionDelay=`${Math.min(i%6,5)*45}ms`});if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const io=new IntersectionObserver((entries,obs)=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');obs.unobserve(e.target)}}),{threshold:.1});els.forEach(e=>io.observe(e))}else els.forEach(e=>e.classList.add('is-visible'));document.querySelectorAll('form').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const msg=form.querySelector('#formMessage')||form.querySelector('p');if(msg)msg.textContent='Thank you — we will be in touch shortly.'}));
